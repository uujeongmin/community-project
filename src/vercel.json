{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
      "cleanUrls": true
    }
  ]
}

{ "routes" : [{
    "handle": "filesystem" }, {"src": "/.*", "dest": "index.html" }]
}
